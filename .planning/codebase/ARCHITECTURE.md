# Architecture
- Monolithic Single Page Application (SPA).
- No backend server; relies purely on client-side rendering.
- `index.html` serves as the entry point and mounts the React application via `app.js`.
- Components are defined within a single `app.jsx` file to simplify structure for this specific use case.
