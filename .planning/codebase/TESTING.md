# Testing
- No formal automated testing framework (e.g., Jest, Cypress) is currently set up.
- Validation is handled primarily via native HTML5 attributes (`minLength`, `pattern`) and React state checks.
- Build testing relies on `esbuild` to verify JSX syntax during compilation.
